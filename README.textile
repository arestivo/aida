h2. Description

Aida is an eclipse plugin that helps developers use unit testing with AOP code.

h2. Installation

Currently Aida only works in Linux. Packages required:

* sudo apt-get install aspectj
* sudo apt-get install graphviz
